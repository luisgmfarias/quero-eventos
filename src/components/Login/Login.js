import React from 'react';
import { LoginStyles } from './styles';

const Login = () => {
  return (
    <LoginStyles>
      // Conteúdo do componente
    </LoginStyles>
  );
};

export default Login;
